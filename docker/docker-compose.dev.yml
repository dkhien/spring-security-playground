services:
  postgres:
    image: postgres:16-alpine
    container_name: security-playground-db
    environment:
      POSTGRES_DB: security_playground
      POSTGRES_USER: playground
      POSTGRES_PASSWORD: playground
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
